# متن ارائه: استقرار Streamlit Cloud و قابلیت‌های v3.1.2

**مدت پیشنهادی:** 8 تا 10 دقیقه برای اسلایدهای استقرار، به‌علاوهٔ 4 تا 5 دقیقه برای مرور محصول v3.1.2.

## بخش اول: اسلایدهای استقرار Streamlit Community Cloud

| اسلاید | متن ارائه |
|---:|---|
| 1. سریع‌ترین مسیر عمومی | «برای انتشار نسخه وب NACA Airfoil Kit Pro بدون نصب محلی و بدون نیاز به Docker، Streamlit Community Cloud ساده‌ترین مسیر است. این سرویس مستقیماً به مخزن GitHub متصل می‌شود؛ یعنی کد، شاخه و فایل ورودی را مشخص می‌کنیم و build توسط سرویس انجام می‌شود. برای دمو، آموزش و نسخه عمومی محصول، این مسیر سریع و کم‌هزینه است. در ادامه، پیش‌نیازهای دقیق مخزن را مرور می‌کنیم تا deployment بدون خطا انجام شود.» |
| 2. پیش‌نیازهای انتشار | «چهار ورودی تعیین‌کننده داریم. نخست، مخزن عمومی `Ali-Marandi/naca-airfoil-kit` است. دوم، شاخه انتشار `main` است که نسخه پایدار پروژه را نگه می‌دارد. سوم، فایل ورودی رابط یعنی `app.py` است. چهارم، `requirements.txt` است که وابستگی‌ها را مشخص می‌کند. پیش از شروع، باید مطمئن شویم حساب GitHub مورد استفاده روی این مخزن دسترسی administrator دارد؛ زیرا Community Cloud باید بتواند مخزن را برای build بخواند.» |
| 3. اتصال GitHub | «در `share.streamlit.io` با حساب GitHub وارد می‌شویم. سپس در Workspace گزینه اتصال حساب GitHub را انتخاب می‌کنیم. صفحه OAuth گیت‌هاب باز می‌شود و باید مجوز Streamlit را تأیید کنیم. برای این مخزن عمومی، دسترسی به همان مخزن کافی است و نیازی به فعال‌کردن دسترسی گسترده به مخزن‌های private نیست؛ مگر آنکه در آینده تصمیم بگیریم نسخه تجاری را خصوصی نگه داریم.» |
| 4. تکمیل فرم Create app | «حالا در فرم Create app، مخزن `Ali-Marandi/naca-airfoil-kit`، شاخه `main` و مسیر اصلی `app.py` را انتخاب می‌کنیم. اگر نام زیردامنه آزاد باشد، `naca-airfoil-kit-pro` یک نام مناسب است. پس از انتخاب گزینه تأیید وجود اپلیکیشن، مقادیر را بازبینی و Deploy را اجرا می‌کنیم. این نقطه، مرز میان پیکربندی و build واقعی است؛ بنابراین از همینجا logهای build را باید به‌عنوان بخشی از فرآیند تحویل مهندسی نگه داریم.» |
| 5. تأیید Build | «پس از پایان build، فقط بازشدن صفحه کافی نیست. ابتدا بررسی می‌کنیم عنوان NACA Airfoil Kit Pro نمایش داده می‌شود. سپس NACA 2412 را با 100 نقطه تولید می‌کنیم و مشاهده می‌کنیم Cl، Cd و L/D ظاهر شوند. تب‌های Geometry، Pressure و Flow Field باید بدون خطا کار کنند. در نسخه v3.1.2، باید QA & Export، Validation، Robustness و Flap Lab را نیز مرور کنیم. اگر build شکست خورد، ابتدا log و `requirements.txt` را بررسی می‌کنیم.» |
| 6. چرخه به‌روزرسانی | «پس از استقرار، GitHub مرکز چرخه انتشار می‌شود. هر push به `main` build جدیدی برای نسخه وب ایجاد می‌کند. اگر وابستگی‌ها تغییر کنند، rebuild انجام می‌شود. URL عمومی را می‌توان با تیم یا کاربران دمو به اشتراک گذاشت. در مرحله production، هیچ token، کلید API یا تنظیم حساس نباید داخل Git نگهداری شود؛ این موارد باید در Secrets پلتفرم hosting ثبت شوند. این تفکیک، هم امنیت را بهتر می‌کند و هم قابلیت بازتولید build را حفظ می‌کند.» |
| 7. مسیر آینده | «Community Cloud برای نسخه عمومی، آموزشی و نمایش محصول مناسب است. اما برای SLA، کاربران هم‌زمان زیاد، domain سازمانی، فایل‌های مطالعه خصوصی یا اجرای solver خارجی مانند XFOIL، باید به یک backend و worker کنترل‌شده یا VPS/container production مهاجرت کنیم. مسیر درست این است که ابتدا product flow و validation را در Community Cloud ثابت کنیم و سپس وقتی نیاز عملیاتی ایجاد شد، اجرای solver و storage را از UI جدا کنیم.» |

## بخش دوم: مرور ارائه‌ای قابلیت‌های v3.1.2

| اسلاید یا بخش پیشنهادی | متن ارائه |
|---|---|
| 1. از screening به مطالعه قابل‌ردیابی | «v3.1.2 صرفاً یک generator هندسی نیست. جریان کار اکنون از geometry و polar شروع می‌شود، به validation و sensitivity می‌رسد و با خروجی‌های مهندسی و audit manifest پایان می‌یابد. با این حال، محصول به‌درستی خروجی‌ها را preliminary screening معرفی می‌کند؛ زیرا مدل پایه هنوز حل‌گر viscous کامل یا جایگزین آزمایش نیست.» |
| 2. Validation Studio | «در Validation Studio، polar آزمایشگاهی CSV را با ستون alpha و Cl/Cd وارد می‌کنیم. سیستم مدل را دقیقاً روی زاویه‌های آزمایش محاسبه می‌کند، نمودار overlay نشان می‌دهد و residual، MAE، RMSE و bias را برای lift و drag جداگانه خروجی می‌گیرد. این تفکیک مهم است، زیرا خوب‌بودن Cl به‌تنهایی به معنی قابل‌قبول‌بودن Cd نیست.» |
| 3. Robustness و Flap Lab | «Robustness به جای یک نقطه Reynolds و roughness، چند شرایط را sweep می‌کند و min/mean/max پاسخ مدل را نشان می‌دهد. بنابراین گزینه‌ای را که فقط در یک شرایط خوب است، با گزینه‌ای که در envelope پایدارتر است مقایسه می‌کنیم. Flap Lab نیز هندسه flap صلب trailing edge را حول hinge انتخابی تغییر می‌دهد. این ابزار برای مقایسه مقدماتی است و اثر gap، seal، لولا و جریان viscous جداشده را ادعا نمی‌کند.» |
| 4. Audit trail و مسیر XFOIL | «قابلیت جدید Audit Manifest، hash هندسه، شرایط عملیاتی، metadata solver و زمان ایجاد study را در JSON ذخیره می‌کند. این ساختار همان پایه لازم برای ادغام XFOIL است. adapter جدید XFOIL فقط commandهای allowlisted می‌سازد، با `shell=False` و در پوشه موقت جدا اجرا می‌شود و failureها را پنهان نمی‌کند. مسیر بعدی، اتصال این adapter به worker تولیدی و اعتبارسنجی آن با datasetهایی مانند NACA 0012 است.» |

## جمع‌بندی نهایی

«نسخه v3.1.2 یک workflow منظم برای غربال‌گری ایرفویل، تحلیل اولیه، اعتبارسنجی داده‌محور، حساسیت چندشرایطی و قابلیت بازتولید مطالعه فراهم می‌کند. برای مرحله بعد، یکپارچه‌سازی XFOIL/QFoil، validation campaign مستند و backend worker سه اولویت اصلی هستند. هدف این نیست که سرعت ابزار جایگزین اعتبار فیزیکی شود؛ هدف، ساختن مسیری است که سرعت و قابلیت ردگیری را با validation معتبر ترکیب کند.»
