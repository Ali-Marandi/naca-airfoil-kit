# راهنمای اعتبارسنجی با داده‌های تونل باد

**NACA Airfoil Kit Pro — نسخهٔ اولیه برای غربال‌گری مهندسی**

> این راهنما یک فرآیند کنترل‌پذیر برای مقایسه خروجی مدل panel/empirical با آزمایش ایجاد می‌کند. توافق با آزمایش در یک بازهٔ محدود، مدل را برای پیش‌بینی خارج از آن بازه، جریان جداشده، گواهی محصول یا تصمیم ایمنی‌محور معتبر نمی‌کند.

## 1. هدف و سطح اعتبار موردنظر

اعتبارسنجی باید این سؤال مشخص را پاسخ دهد: «برای این **هندسهٔ دقیق**، در این **شرایط جریان ثبت‌شده** و در این **بازهٔ زاویهٔ حمله**، اختلاف Cl و Cd مدل با measurement چه اندازه است؟» بنابراین هیچ عدد عمومی از نوع «دقت مدل» بدون نام airfoil، Reynolds، roughness و بازهٔ alpha گزارش نشود.

| سطح کاربرد | حداقل مدرک لازم | وضعیت مدل فعلی |
|---|---|---|
| آموزش و غربال‌گری کانسپت | polar اولیه، بررسی هندسه و sanity check | مناسب، با برچسب preliminary |
| انتخاب اولیه airfoil | تطبیق محدود با dataset قابل‌ردیابی در Re نزدیک | مناسب، مشروط به ثبت اختلاف |
| پیش‌بینی نزدیک stall/پس از stall | دادهٔ آزمایش یا حل‌گر viscous معتبر در همان Re و transition | نیازمند اعتبارسنجی خارجی |
| طراحی ایمنی‌محور، گواهی یا بار نهایی | آزمایش/CFD تأییدشده، عدم‌قطعیت و مرور مستقل | خارج از دامنهٔ ابزار |

## 2. انتخاب دادهٔ مرجع

برای NACA 0012، گزارش McCroskey بیش از 40 تونل باد را به‌صورت انتقادی بررسی کرده و تصریح می‌کند که هیچ آزمایش واحدی همهٔ داده‌های قابل‌اعتماد را در همهٔ شرایط پوشش نمی‌دهد؛ از این رو dataset انتخابی باید به‌همراه دامنهٔ کاربرد خود نگهداری شود. [1] آزمایش NACA TR-647 نیز برای NACA 0012 داده‌هایی در رینولدزهای حدود 1.7 تا 7.0 میلیون ثبت کرده و برای بررسی اثر scale و turbulence طراحی شده است. [2]

برای NACA 2412، NASA CR-197497 جریان جداشده را در Re = 2.2×10⁶ و M = 0.13 در alphaهای 12.4°، 14.4° و 16.4° بررسی کرده است و فشار، wake و دادهٔ لایهٔ مرزی را نیز ارائه می‌دهد؛ این منبع برای کنترل ناحیهٔ نزدیک جدایش مناسب است. [3] برای بازهٔ low-Re و زاویهٔ حملهٔ کامل نیز دیتاست Airfoil 360 v2022 برای NACA 0012 و 2412 در Re = 50,000 و 100,000، دادهٔ Cl و Cd تا 360° ارائه می‌کند و تصحیحات استاندارد تونل باد را گزارش می‌کند. [4]

| نمونه منبع | مناسب برای | محدودیت مهم |
|---|---|---|
| Abbott & von Doenhoff، *Theory of Wing Sections* | benchmark کلاسیک polar برای مقاطع NACA | باید صفحه/شرط دقیق digitize و ثبت شود |
| NASA TM-100019 | انتخاب dataset معتبر NACA 0012 و تحلیل اختلاف تونل‌ها | مرجع meta-analysis است، نه جایگزین raw data |
| NASA CR-197497 | NACA 2412 نزدیک جدایش در Re و Mach مشخص | محدود به شرایط و alphaهای آزمایش |
| Airfoil 360 v2022 | low-Re و پس از stall/360° | نباید با Re بالا بدون تطبیق مقایسه شود |

## 3. قرارداد داده و metadata اجباری

فایل آزمایشی برای تب **Validation** باید دست‌کم `alpha_deg` و یکی از `cl` یا `cd` داشته باشد. ستون‌های پذیرفته‌شده برای alpha عبارت‌اند از `alpha_deg`، `alpha`، `aoa` و `angle_of_attack`. نمونهٔ حداقلی:

```csv
alpha_deg,cl,cd
-2.0,0.031,0.0084
0.0,0.248,0.0072
2.0,0.463,0.0070
```

خود CSV باید به یک رکورد metadata مستقل یا header مستند پیوند بخورد. بدون موارد زیر، نتیجه صرفاً **informational** است:

| metadata | دلیل ثبت |
|---|---|
| نام/نسخه هندسه، chord و وضعیت flap | جلوگیری از مقایسهٔ دو شکل غیرهمسان |
| Reynolds، Mach و خواص هوا | تعریف تشابه دینامیکی و تراکم‌پذیری |
| transition، roughness و turbulence intensity | اثر مستقیم بر Cd و جدایش |
| نوع تونل، blockage/wall corrections و روش مرجع area | جلوگیری از خطای systematic facility |
| convention زاویه، tare و روش محاسبه نیرو | هم‌علامت‌شدن alpha، Cl و Cd |
| منبع، صفحه/شناسه dataset و مجوز استفاده | قابلیت ردیابی و بازتولید |

## 4. فرآیند پیشنهادی، گام‌به‌گام

### گام 1 — قفل‌کردن هندسه

مختصات test article را وارد یا از NACA تولید کنید. تعداد نقاط، trailing-edge treatment، flap و روش normalization را در پرونده مطالعه ثبت کنید. در تب **QA & Export** ضخامت، camber و trailing-edge gap را صادر کنید و با مشخصات نمونه آزمایش تطبیق دهید.

### گام 2 — تطبیق شرایط

Reynolds نرم‌افزار را برابر مقدار آزمایش قرار دهید. به‌صورت پیش‌فرض فقط آزمون‌هایی با اختلاف Reynolds کمتر از ±5٪ و اختلاف Mach کمتر از 0.02 با یکدیگر مقایسه شوند؛ در صورت عدم امکان، اختلاف را در گزارش با صراحت ثبت کنید. roughness ورودی باید توصیف سطح نمونه را بازتاب دهد؛ این پارامتر را برای جبران مصنوعی اختلاف Cd tune نکنید.

### گام 3 — جداسازی ناحیه‌های جریان

نقاط را به سه گروه تقسیم کنید: محدودهٔ خطی پیش از stall، نزدیک stall، و پس از stall/جدایش. نتیجهٔ RMS پیش از stall باید جداگانه گزارش شود؛ مخلوط کردن آن با نقاط جداشده یک عدد گمراه‌کننده تولید می‌کند، زیرا مدل کنونی حل‌گر viscous جداشدگی نیست.

### گام 4 — مقایسه در نرم‌افزار

در تب **Validation** فایل CSV را بارگذاری کنید، Reynolds آزمایش را وارد کنید و جدول residual را صادر کنید. سامانه نقاط مدل را دقیقاً در alphaهای آزمایش محاسبه می‌کند و برای Cl و Cd این شاخص‌ها را می‌سازد:

| شاخص | تعریف | تفسیر |
|---|---|---|
| MAE | میانگین قدرمطلق خطا | اندازه خطای معمول بدون حساسیت شدید به outlier |
| RMSE | ریشه میانگین مربع خطا | حساس به خطاهای بزرگ، مناسب برای کنترل بدترین نواحی |
| Bias | میانگین `(model − experiment)` | تشخیص بیش‌/کم‌پیش‌بینی systematic |
| MAPE | خطای نسبی میانگین، فقط برای مقدار غیرصفر | برای Cl نزدیک صفر با احتیاط استفاده شود |

### گام 5 — تحلیل علّت اختلاف

اگر Cl اختلاف دارد، ابتدا alpha-zero-lift، convention زاویه، camber واقعی، wall correction و Re را بررسی کنید. اگر Cd اختلاف دارد، transition، roughness، wake-survey/force-drag method، blockage، سطح زبری و محدودیت مدل empirical را پیش از تغییر تنظیمات کنترل کنید. تنها در صورت وجود مرجع مستقل، یک model correction قابل‌استفاده است؛ correction باید dataset، دامنه Re و تاریخ نسخه را همراه خود داشته باشد.

### گام 6 — ثبت و تصمیم‌گیری

فایل CSV، خروجی residual، شرایط ورودی، hash یا شناسه commit، نسخه مدل و منبع آزمایش را در یک پوشه study نگه دارید. جمع‌بندی باید فقط به حوزه‌ای تعمیم یابد که واقعاً آزموده شده است. برای مثال: «در NACA 2412، Re = 1.0×10⁶، alpha = −2° تا 8°، با سطح smooth، مدل دارای Cl RMSE برابر … بود»؛ نه «مدل دقیق است».

## 5. استفاده از پاکت حساسیت

تب **Robustness** برای حساسیت قطعی مدل به چند مقدار Reynolds و roughness طراحی شده است. خروجی min/mean/max برای Cl، Cd و L/D در مجموعه شرایط انتخابی می‌دهد. این پاکت، **بازهٔ اطمینان آماری** یا uncertainty measurement نیست. برای یک مطالعه آزمایشگاهی، عدم‌قطعیت ابزار اندازه‌گیری، تکرارپذیری، تصحیحات دیواره و uncertainty شرایط محیطی باید جداگانه با روش آزمایشگاه محاسبه شود.

## 6. معیارهای پذیرش پیشنهادی

هیچ آستانهٔ پذیرش جهانی وجود ندارد. آستانه باید متناسب با مأموریت، Re، ناحیهٔ alpha و کیفیت داده تعریف شود. جدول زیر یک قالب تصمیم است، نه specification عمومی:

| ناحیه | شاخص اجباری | سیاست پیشنهادی |
|---|---|---|
| پیش از stall | Cl RMSE، Cd RMSE، bias و Δα0 | آستانه پروژه را پیش از مشاهده نتایج تعیین کنید |
| نزدیک stall | اختلاف Clmax، alpha در Clmax، محل شروع جدایش | فقط با دادهٔ viscous/آزمایش هم‌شرایط قضاوت کنید |
| پس از stall | شکل curve، sign و دامنه بارها | به مدل فعلی اتکا نکنید؛ از dataset یا solver مناسب استفاده کنید |
| انتخاب میان چند airfoil | رتبه‌بندی در چند Re/roughness و validation محدود | از یک نقطه L/D برای تصمیم نهایی استفاده نکنید |

## 7. نمونه برنامهٔ validation برای نسخه بعدی

یک campaign قابل‌اجرا شامل NACA 0012 و 2412 است. ابتدا data points مرجع را فقط پس از digitization با کنترل دوم وارد کنید. سپس برای هر airfoil یک ناحیهٔ low-alpha و یک ناحیهٔ نزدیک stall تعریف کنید؛ محاسبه را با Re منطبق تکرار کنید؛ CSV residualها را صادر کنید؛ و نتایج را با metadata و domain-of-validity منتشر کنید. در مرحله بعد، backend اختیاری XFOIL/QFoil یا solver viscous معادل می‌تواند به‌عنوان comparator افزوده شود، اما هم‌چنان جایگزین validation تجربی نخواهد بود.

## منابع

[1]: https://ntrs.nasa.gov/citations/19880002254 "McCroskey, A Critical Assessment of Wind Tunnel Results for the NACA 0012 Airfoil, NASA TM-100019 (1987)"
[2]: https://ntrs.nasa.gov/citations/19930091723 "Goett & Bullivant, Tests of NACA 0009, 0012, and 0018 Airfoils in the Full-Scale Tunnel, NACA TR-647 (1938)"
[3]: https://ntrs.nasa.gov/citations/19950002355 "Seetharam, Rodgers & Wentz, Experimental Studies of Flow Separation of the NACA 2412 Airfoil at Low Speeds, NASA CR-197497 (1997)"
[4]: https://data.mendeley.com/datasets/dz4bv26ncd "Stringer, Airfoil 360 v2022: Wind Tunnel Data (2022), DOI 10.17632/dz4bv26ncd.1"
